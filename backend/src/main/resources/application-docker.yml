spring:
  thymeleaf:
    cache: false

logging:
  level:
    root: INFO
    com.meridian.portal: INFO

server:
  servlet:
    session:
      timeout: 30m

meridian:
  auth:
    bootstrap-admin-reset-on-startup: true
